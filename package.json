{
  "name": "resume",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "build:pdf": "node scripts/generate-pdf.js",
    "build:all": "astro build && node scripts/generate-pdf.js",
    "build:all-themes": "node scripts/generate-all-pdfs.js"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "puppeteer": "^23.0.0"
  }
}
